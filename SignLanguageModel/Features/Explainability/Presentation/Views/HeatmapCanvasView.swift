import SwiftUI

struct HeatmapCanvasView: View {
    var body: some View {
        Canvas { context, size in
            // Heatmap rendering using Canvas
        }
        .frame(height: 300)
    }
}
